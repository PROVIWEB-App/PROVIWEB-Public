// Este script ya no es necesario - el build se hace directamente en public/
// vite.config.js está configurado con outDir: './dist' que apunta a public/dist
console.log('[prepare-dist] Este script está obsoleto. El build se genera directamente en public/dist');
